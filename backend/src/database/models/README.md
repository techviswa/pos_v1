Database model files belong in this directory when the backend is connected to a persistent datastore.

The current backend uses in-memory repositories for scaffolded CRUD behavior, so this folder is intentionally reserved for future model implementations.
